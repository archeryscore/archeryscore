caricaProgressi();async function caricaProgressi(){const r=await fetch("/api/progressi");const p=await r.json();const el=document.getElementById("progressContainer");if(!p.eventi){el.innerHTML='<div class="empty-state">Dati non sufficienti</div>';return}el.innerHTML=`<div class="record-card"><div class="record-header"><div><h2>Andamento generale</h2><p>Confronto fra prima metà e seconda metà</p></div><div class="record-badge">${p.eventi} eventi</div></div><div class="record-grid"><div class="record-box"><span>Media prima</span><strong>${p.media_prima}</strong></div><div class="record-box"><span>Media seconda</span><strong>${p.media_seconda}</strong></div><div class="record-box"><span>Differenza</span><strong class="${p.differenza_media>=0?"progress-good":"progress-bad"}">${p.differenza_media>=0?"+":""}${p.differenza_media}</strong></div><div class="record-box"><span>Media/freccia prima</span><strong>${p.ppf_prima}</strong></div><div class="record-box"><span>Media/freccia seconda</span><strong>${p.ppf_seconda}</strong></div></div></div><div class="record-card"><h2>Migliori miglioramenti</h2>${p.migliori_trend.map(t=>`<p><strong>${t.nome}</strong>: <span class="progress-good">${t.differenza>=0?"+":""}${t.differenza}</span> (${t.primo} → ${t.ultimo})</p>`).join("")||"Nessun dato"}</div><div class="record-card"><h2>Da tenere d'occhio</h2>${p.peggiori_trend.map(t=>`<p><strong>${t.nome}</strong>: <span class="${t.differenza>=0?"progress-good":"progress-bad"}">${t.differenza>=0?"+":""}${t.differenza}</span> (${t.primo} → ${t.ultimo})</p>`).join("")||"Nessun dato"}</div>`;}